export const generateScrollingConfig1 = (animated, index) => {
	return {
		animated: animated,
		index: index,
	}
};
export const generateScrollingConfig = (animated, index) => {
	return {
		animated: animated,
		index: index,
	}
};

export const scrollToEndConfig = {
	animated: true,
	duration: 500,
};

export const scrollToEndConfig1 = {
	animated: true,
	duration: 10,
};